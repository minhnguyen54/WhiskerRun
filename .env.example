# WhiskerRun does not require environment variables for local gameplay.
# Keep this file as a safe template if future services are added.

# Example future use:
# ANALYTICS_API_KEY=
# LEADERBOARD_API_URL=
